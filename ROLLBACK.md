# Saved version and rollback

The version before this experiment is preserved at commit `df0e323e8f8250200fc0775368d97c5eebaf567e`, tagged [`before-structure-v1-20260910`](https://github.com/blu-dog-longevity/dog-matching-review/tree/before-structure-v1-20260910).

Ask Logan's coding agent to roll back the linked-report prototype. The release can be reverted with a new Git commit and republished; there is no need to erase repository history. The baseline source, current review assets, and the local mapping/build files have a separate retained recovery snapshot.

This experiment adds context-aware symptom evidence and selected source-linked report details to the existing review tabs. Two previously unmapped CBD oil uses, in source rows 32 and 38, are added to counting; two oils within one report still count once for CBD oil and once for cannabis. Product details do not multiply support counts. The original workbook and raw treatment inventory are unchanged.

This is the review prototype only. It does not write to the production platform or database.
